public class Box {
    // Box 클래스의 필드 생성
    int width, length, height;

    // BoxTest 에서 필요한 생성자를 초기화 한다
    Box(int width, int length, int height){
        this.width = width;
        this.length = length;
        this.height = height;
    }
}
